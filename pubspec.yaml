name: app_stream
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.8.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  video_player: ^2.8.1
  cupertino_icons: ^1.0.6
  chewie: ^1.7.4
  path_provider: ^2.1.1
  youtube_player_flutter: ^9.0.1
  firebase_core: ^3.14.0
  firebase_auth: ^5.6.0
  firebase_database: ^11.3.7
  firebase_storage: ^12.4.7
  image_picker: ^1.0.4
  http: ^1.1.0 
  icons_launcher: ^3.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.13.1

# Configuración de iconos
flutter_icons:
  android: true
  ios: true
  image_path: "assets/images/fondo.png"
  

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/videos/